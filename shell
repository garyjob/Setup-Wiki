Copy some stuff to clipboard from terminal
    cat somefile | pbcopy 

Copy some stuff to clipboard from remote ssh console
    somefile | ssh desktop pbcopy

Curl test for pingback
  curl -X POST -d @payload.txt https://blog.disconnect.me/xmlrpc.php

Curl test for private search
  curl -H 'Cookie: __beta=blimply123' -I https://staging.disconnect.me/-hide-/https://www.google.com/blank.html
