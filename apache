To start process
sudo httpd

To kill process
ps aux | grep httpd

sudo kill -9 pid

Location of local Apache Server
  /etc/apache2

Adding new sites to local Apache Server
=======================================
  create new site configuration file at
    /etc/apache2/sites/
 
  restart apache server
    sudo httpd -k restart

